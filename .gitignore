# Dependencias
node_modules/

# Builds y paquetes
portable-build/
build-app/
dist/
*.zip

# DATOS DE WALLET — NUNCA subir
MonederoDNNS-datos/
Koberlet-datos/
vault.json
vault-*.bak.json
history-cache.json
config.json
portable.flag

# Claves y secretos
TeamRed.json
llaves/
claves/
*.key
*.pem
.env
.env.*
.keys/
_backups/

_seed.txt
diag-deriv.js
.publicar-tmp/
dist-installer/

# Destino de publicacion (servidor y rutas reales de quien publica)
.publicar.conf

# Bitacora interna: lleva rutas de otros proyectos y negocio de terceros.
# Se queda en el PC de quien publica, no en el repositorio.
CUADERNO.md

# Informes internos de auditoria de secretos (citan infraestructura real)
AUDITORIA_SECRETOS_*.md
